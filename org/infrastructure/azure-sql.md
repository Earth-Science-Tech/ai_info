# Azure SQL Server

## Connection Details

- **Server:** liberty-link.database.windows.net
- **Database:** liberty_link_stage
- **Platform:** Azure SQL Server (SQL Server compatible)
- **Authentication:** SQL authentication (emed_app / emed_etl users)

## Database Users

| User | Purpose | Connection From |
|------|---------|----------------|
| `emed_app` | Node.js web application | Azure App Service |
| `emed_etl` | Python ETL scripts | Job servers, Prefect workers |
| Admin | Schema changes, user management | SSMS / Azure Data Studio only |

## Table Organization (82+ tables)

| Prefix | Count | Domain |
|--------|-------|--------|
| `moct_*` | 25 | Core medical office consultation tracking |
| `rxcs_*` | 19 | Rx Compound Store pharmacy data |
| `emed_*` | 15 | Users, email, SMS, billing, pricing |
| `mmed_*` | 13 | Mister Meds pharmacy data |
| `mdvo_*` | 18 | Meduvo pharmacy data + pricing |
| `woo_*` | 4 | WooCommerce integration |
| `wpforms_*` | 2 | WPForms patient questionnaires |
| Other | 4+ | sessions, etl_metadata, sql_log, etc. |

## Views and Stored Procedures

- **19 views:** `view_emed_*`, `view_mmed_*`, `view_mdvo_*`, `view_moct_*`, `view_rxcs_*`, `view_peaks_*`, `view_woo_*`
- **18 stored procedures:** `usp_etl_*` (ETL), `usp_insert_*`, `usp_normalize_*`, `usp_refresh_*`

## Schema Documentation

Auto-generated by `emed_app/python/extract_schema.py`:
- Output: `emed_app/sql/info.claude`
- Contains: all table schemas, columns, types, relationships, views, stored procedures
- Refresh: `python python/extract_schema.py` from emed_app root

## Connection Patterns

### Node.js (emed_app)
Uses `mssql` package with connection pooling via `server/sql.js`

### Python (emed_etl)
Uses `pyodbc` via `etl/shared/db.py` utility module
